import Image from "next/image";
import Link from "next/link";
import Breadcrumb from "@/components/navigation/Breadcrumb";
import mechanicsData from "@/data/game/mechanics.json";
import { createMetadata } from "@/seo/metadata";
import styles from "@/style/page/archive/archive.module.css";
export const metadata = createMetadata("Goodly Trials Mechanics", "Learn Goodly Trials stats, formation, flanking, modes, markets, and the twelve-trial journey.", "/wiki/mechanics");
export default function MechanicsPage() { return <main><section className={styles.hero}><Image className={styles.heroImage} src="/images/game/screenshot-6.webp" alt="Goodly Trials advanced training diagrams" fill preload sizes="100vw" /><div className={styles.heroShade} /><div className={`container ${styles.heroContent}`}><Breadcrumb items={[{ label: "Home", href: "/" }, { label: "Wiki", href: "/wiki" }, { label: "Mechanics" }]} /><p className={styles.eyebrow}>How the road works</p><h1>Goodly Trials Mechanics</h1><p>Start with modes and card anatomy, then move into the placement decisions that matter before auto-battle begins.</p></div></section><section className="container section"><div className={styles.portalGrid}>{mechanicsData.map((entry) => <Link className={styles.portalCard} href={`/wiki/mechanics/${entry.slug}`} key={entry.slug}><span>⌘</span><h2>{entry.title.replace("Goodly Trials ", "")}</h2><p>{entry.summary}</p><b>Read guide →</b></Link>)}</div><section className={styles.sectionBlock}><h2>The strategic loop</h2><p>The official game description presents a repeating Shop → Place → Equip → Battle → Plan loop. The road waits while you think, but once the company is committed, the line proves the strength of the plan without hurried commands.</p></section></section></main>; }
