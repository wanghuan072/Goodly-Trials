import Image from "next/image";
import Link from "next/link";
import Breadcrumb from "@/components/navigation/Breadcrumb";
import { builds, items, units } from "@/lib/data/game-content";
import type { Item } from "@/types/content";
import styles from "@/style/page/wiki/detail.module.css";

const unitMatches: Record<string, string[]> = {
  "iron-sword-of-brawn": ["goodly-knight", "portly-knight"], "iron-shield-of-brawn": ["goodly-knight", "portly-knight"], "bow-of-grace": ["archer"],
  "feather-charm-of-reflex": ["archer", "skeleton-dog"], "wand-of-wit": ["wizard"], "ruby-charm-of-wit": ["wizard"], "frostfall-of-lore": ["wizard"], "fire-palm-of-balance": ["wizard"],
};

export default function ItemDetailPage({ item }: { item: Item }) {
  const matchedUnits = (unitMatches[item.slug] ?? []).map((slug) => units.find((unit) => unit.slug === slug)).filter(Boolean);
  const relatedItems = items.filter((entry) => entry.slug !== item.slug && (entry.type === item.type || entry.type.split(" ")[0] === item.type.split(" ")[0])).slice(0, 3);
  const relatedBuilds = builds.filter((build) => matchedUnits.some((unit) => unit?.slug === build.unitSlug));
  return <main className={`container ${styles.detailShell}`}><Breadcrumb items={[{ label: "Home", href: "/" }, { label: "Wiki", href: "/wiki" }, { label: "Items", href: "/wiki/items" }, { label: item.name }]} /><header className={styles.itemHero}><div className={styles.itemVisual}><Image src={item.image} alt={`${item.name} official game icon`} width={120} height={120} unoptimized={item.image.endsWith(".gif")} /></div><div className={styles.itemCopy}><span className={styles.badge}>{item.type}</span><h1>{item.name}</h1><div className={styles.badges}><span>{item.cost}G</span>{item.requirements && <span>Requires {item.requirements}</span>}<span>{item.gameVersion}</span></div><div className={styles.effectList}>{item.effects.map((effect) => <span key={effect}>{effect}</span>)}</div></div></header><div className={styles.detailGrid}><article className={styles.detailMain}><section className={styles.panel}><h2>Overview</h2><p>{item.name} is a verified {item.type.toLowerCase()} item listed in the official Goodly Trials item guide for {item.gameVersion}. Its public price is {item.cost}G{item.requirements ? ` and its listed requirement is ${item.requirements}` : " with no requirement shown in the public record"}.</p></section><section className={styles.panel}><h2>Requirements, Stats & Effects</h2><table className={styles.statsTable}><thead><tr><th>Type</th><th>Requirement</th><th>Cost</th><th>Version</th></tr></thead><tbody><tr><td>{item.type}</td><td>{item.requirements ?? "Not listed"}</td><td>{item.cost}G</td><td>{item.gameVersion}</td></tr></tbody></table><ul>{item.effects.map((effect) => <li key={effect}>{effect}</li>)}</ul></section><section className={`${styles.panel} ${styles.editorial}`}><h2>Editorial Unit Fit</h2><p>Recommendation layer. Matching below is based on current public attributes and requirements, not a developer-authored tier list.</p>{matchedUnits.length ? <div className={styles.relatedGrid}>{matchedUnits.map((unit) => unit && <Link key={unit.slug} href={`/wiki/units/${unit.slug}`}><b>{unit.name}</b><span>{unit.faction} · {unit.stats.str}/{unit.stats.agi}/{unit.stats.int} STR/AGI/INT</span></Link>)}</div> : <p>No unit recommendation is published until a verified attribute and mechanic case can be made.</p>}</section>{relatedBuilds.length > 0 && <section className={`${styles.panel} ${styles.editorial}`}><h2>Build Notes Using This Item</h2><div className={styles.relatedGrid}>{relatedBuilds.map((build) => <Link key={build.slug} href={`/builds/${build.slug}`}><b>{build.title}</b><span>{build.version} · {build.difficulty}</span></Link>)}</div></section>}<section className={styles.panel}><h2>Related Items</h2><div className={styles.relatedGrid}>{relatedItems.map((entry) => <Link key={entry.slug} href={`/wiki/items/${entry.slug}`}><b>{entry.name}</b><span>{entry.type} · {entry.cost}G</span></Link>)}</div></section></article><aside className={styles.sideProfile}><h2>Item profile</h2><dl><div><dt>Type</dt><dd>{item.type}</dd></div><div><dt>Requirement</dt><dd>{item.requirements ?? "Not listed"}</dd></div><div><dt>Cost</dt><dd>{item.cost}G</dd></div><div><dt>Version</dt><dd>{item.gameVersion}</dd></div><div><dt>Verified</dt><dd>{item.lastVerified}</dd></div></dl><h3>Explore</h3><Link href="/wiki/items">All items</Link><Link href="/wiki/units">All units</Link><Link href="/builds">Build notes</Link><h3>Primary source</h3><a href={item.source} target="_blank" rel="noreferrer">Official item guide ↗</a></aside></div></main>;
}
